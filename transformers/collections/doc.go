/*
Package collections is a medusa transformer that group files
together in collections.

It stores the collections in a [Collections] type at
the "collections" key in the global store.
*/
package collections
